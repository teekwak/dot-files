[alias]
    alias = "!git config -l | grep alias | cut -c 7-"
    ba = branch -a
    cb = checkout -b
    co = checkout -- -A
    cm = commit -m
    log = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --numstat
    sk = stash -k
    s = status -s
